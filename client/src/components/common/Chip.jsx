import React from 'react';

const Chip = ({ label }) => <p className='text-xs bg-gradient-to-r from-blue-500 to-blue-300 text-white py-1 px-2 rounded-full capitalize'>{label}</p>;

export default Chip;
